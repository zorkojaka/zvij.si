# Pages in Scope (Discovery)

## Identified page types (to confirm against production URL)

Based on the WordPress theme and installed plugins, the following pages are expected:

- Home (`/`)
- About / O nas (`/o-nas`)
- Offer / Shop (`/ponudba` or `/shop`)
- Product detail pages (`/product/*`)
- Contact (`/kontakt`)
- Blog / News (`/blog`)
- Single post (`/blog/*`)
- Privacy policy (`/politika-zasebnosti`)
- Terms / Cookies (`/pogoji` or `/piskotki`)
- Search results (`/search`)
- 404 (`/404`)

## UI component inventory (initial)

- Global header with navigation + mobile hamburger
- Hero section with primary CTA
- Product grid / listing (WooCommerce)
- Product detail layout (gallery, price, CTA)
- Content blocks (text + image, split sections)
- Slider / carousel (RevSlider)
- Newsletter or CTA block
- Contact form (Contact Form 7)
- Footer with contact info + social links
- Cookie / consent banner

## Hard parts / plugin dependencies

- RevSlider (slider/carousel behavior)
- WooCommerce (catalog + PDP + cart)
- Contact Form 7 (form validation + submission)
- Elementor blocks (custom layout sections)

> Next step: crawl live production URL to confirm exact pages, URLs, and component variants.
